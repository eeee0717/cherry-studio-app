import type { ReactElement } from 'react';

export type PaintingZoomLinkProps = {
  children: ReactElement;
  fileEntryId: string;
  paintingId: string;
};

export type PaintingZoomTargetProps = {
  children: ReactElement;
};
